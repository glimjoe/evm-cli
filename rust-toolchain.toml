[toolchain]
# Pinned per ADR-0001 + PLAN-V9 §18: MSRV is 1.96, toolchain matches.
# Earlier (PLAN-V5..V8) the toolchain was temporarily pinned to 1.96.0
# while MSRV was declared as 1.91; this was inconsistent and resolved
# on 2026-06-11 by raising the declared MSRV to 1.96 to match.
channel = "1.96.0"
